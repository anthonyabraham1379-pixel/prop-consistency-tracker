import React from 'react';

// react-native-google-mobile-ads no tiene soporte web; en el preview de
// navegador simplemente no se muestra nada (los anuncios solo son relevantes
// en el build nativo de Android/iOS).
export default function AdBanner() {
  return null;
}
